// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("phptrace", "for phptrace support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("phptrace", "enable phptrace support", "no");

if (PHP_PHPTRACE != "no") {
	EXTENSION("phptrace", "phptrace.c");
}

